{
  "//Serilog": [
    "Levels live here rather than under Logging:LogLevel because Serilog owns the",
    "pipeline now — the default providers are removed at startup so that nothing can",
    "write a log line that skipped the PII redaction step."
  ],
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.Hosting.Lifetime": "Information"
      }
    }
  }
}
