<!--
SPDX-FileCopyrightText: 2026 Max Veregge
SPDX-License-Identifier: AGPL-3.0-or-later
-->
<Project Sdk="Microsoft.NET.Sdk">

  <!-- Entities only, and deliberately no EF Core dependency. Persistence lives in
       Dbr.Infrastructure so that the self-hosted and hosted composition roots have a
       clean layer to depend on when they are split apart, rather than needing to be
       untangled from the entity definitions later. -->

</Project>
